# HW 4.3: Applied Data Visualizations Portfolio

## Project Plan & Workflow
**Repository Maintenance Plan:**
* Use the `main` branch as the definitive, stable version of the project.
* Create separate feature branches for making edits (e.g., updating documentation or adding new files).
* Track required tasks using the GitHub **Issues** tab.
* Integrate changes back into `main` exclusively through formal **Pull Requests** to maintain version control history.

**HW #4.3 Execution Plan:**
* Gather previous code and narrative text from HW #3.4 (Busiest Airports and Monte Carlo Simulations).
* Extract the newly provided `calcium.csv` dataset and clean it using `tidyverse` (pivot_longer, handle NAs).
* Develop a specific, plan-based prompt to feed into a GenAI tool to assist with the plotting code.
* Compare the GenAI output of a generic prompt vs. the plan-based prompt.
* Render the final `.qmd` file into a clean HTML/PDF document.
